import { Injectable } from '@angular/core';
import * as signalkJsClient from 'signalk-client';

@Injectable()
export class SignalKService {

    connectSignalK(host:string) {
        //var client = new signalkJsClient.client('localhost');

        return 3
    }
    
}