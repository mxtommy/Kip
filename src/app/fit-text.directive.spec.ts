import { FitTextDirective } from './fit-text.directive';

describe('FitTextDirective', () => {
  it('should create an instance', () => {
    const directive = new FitTextDirective(el);
    expect(directive).toBeTruthy();
  });
});
